{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Core permissions for the Kernex main window",
  "windows": ["main"],
  "permissions": ["core:default", "dialog:allow-open"]
}
